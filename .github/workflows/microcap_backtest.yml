name: Actions Smoke Test

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm runner
        run: |
          python --version
          echo "actions-ok"
